services:
  librarian:
    build: .
    image: the-librarian:latest
    container_name: the-librarian
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    environment:
      LIBRARIAN_ADMIN_TOKEN: "${LIBRARIAN_ADMIN_TOKEN:?Set LIBRARIAN_ADMIN_TOKEN in .env}"
      LIBRARIAN_AGENT_TOKEN: "${LIBRARIAN_AGENT_TOKEN:-}"
      LIBRARIAN_AGENT_TOKENS: "${LIBRARIAN_AGENT_TOKENS:-}"
      LIBRARIAN_ALLOWED_ORIGINS: "${LIBRARIAN_ALLOWED_ORIGINS:-}"
      LIBRARIAN_DATA_DIR: "/data"
      LIBRARIAN_HOST: "0.0.0.0"
      LIBRARIAN_PORT: "3838"
    ports:
      - "${LIBRARIAN_PUBLISHED_HOST:-127.0.0.1}:3838:3838"
    volumes:
      - ./data:/data
    tmpfs:
      - /tmp
